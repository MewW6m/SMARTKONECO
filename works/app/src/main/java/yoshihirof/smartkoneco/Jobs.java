package yoshihirof.smartkoneco;

public class Jobs {
/*
    public class kyuko extends BroadcastReceiver {

        @Override
        public void onReceive(Context context, Intent intent) {
            NotificationManager notificationManager = (NotificationManager)context.getSystemService(Context.NOTIFICATION_SERVICE);
            int id = intent.getIntExtra("noticekyuko", 0);
            String title = "休講情報（木曜日3限）";
            String content = "4-304 コンピューターワークショップ";
            notificationManager.notify(id, notice(context, title,content));

        }
    }
    public class getrooms extends BroadcastReceiver {

        @Override
        public void onReceive(Context context, Intent intent) {

        }
    }

    public class getplan extends BroadcastReceiver {

        @Override
        public void onReceive(Context context, Intent intent) {

        }
    }

    public class getnews extends BroadcastReceiver {

        @Override
        public void onReceive(Context context, Intent intent) {

        }
    }

    public class noticeclass extends BroadcastReceiver {

        @Override
        public void onReceive(Context context, Intent intent) {
            NotificationManager notificationManager = (NotificationManager)context.getSystemService(Context.NOTIFICATION_SERVICE);
            int id = intent.getIntExtra("noticeclass", 0);
            String title = "授業10分前情報（9月13日木曜日3限）";
            String content = "4-304 コンピューターワークショップ";
            notificationManager.notify(id, notice(context, title,content));

            //NotificationManager mNotificationManager = (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);
            //notificationManager.cancel(id);
        }
    }


    private Notification notice(Context context,String title, String content) {
        Notification.Builder builder = new Notification.Builder(context);
        builder.setSmallIcon(R.mipmap.ic_smartkoneco)
                .setContentTitle(title)
                .setContentText(content)
                .setWhen(Calendar.getInstance().getTimeInMillis())
                .setAutoCancel(true)
                .setDefaults(Notification.DEFAULT_ALL);

        return builder.build();
    }
    */
}